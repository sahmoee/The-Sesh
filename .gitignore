# Secrets — NEVER commit
Secrets.xcconfig
Secrets.local.xcconfig
*.secret.xcconfig
.env
.env.*
!.env.example
.dev.vars
*.p8
*.p12
*.cer
*.pem
*.key
*.mobileprovision
AuthKey_*.p8
GoogleService-Info.plist
# Cloudflare worker local state (secrets live in Cloudflare)
_worker/**/.wrangler/
**/.wrangler/
# Xcode
build/
DerivedData/
.bbderived/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM
# SwiftPM
.build/
.swiftpm/
# Node
node_modules/
# macOS
.DS_Store

# macOS AppleDouble resource forks (created by zip/unzip round-trips)
._*
.AppleDouble

# Local build snapshots — keep on disk if you want them, never in git
.buildbuddy-backups/

# One-off patch exports are reproducible from git history
*.diff

# Worker has no package.json; a stray lockfile is always an artifact
_worker/**/package-lock.json

# Local operating guide — not part of the shipped repo
README-FIRST.md
IMPROVEMENTS.md
LOUNGE_R2_PHASE4_NOTES.md
NOTES.md
